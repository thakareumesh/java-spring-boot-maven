---
- hosts: dev
  become: true
  tasks:
   - name: start docker
     service:
       name: docker
       state: started
       enabled: yes
   - name: start the container
     docker_container:
       name: vertech
       image: "umesh4321/vertech:{{DOCKER_TAG}}"
       state: started
       published_ports:
       - 0.0.0.0:8080:8080
       command: sleep infinity
       tty: true
       detach: true
